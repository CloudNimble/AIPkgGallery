---
title: OpenCode
description: Platform specification for OpenCode, the open-source terminal-native AI coding agent by Anomaly
icon: terminal
---

## Overview

[OpenCode](https://opencode.ai/) is an open-source, terminal-native AI coding agent developed by **Anomaly** (Anomaly Innovations), the team behind SST (Serverless Stack) and terminal.shop. Licensed under the MIT License, OpenCode provides a rich TUI (Terminal User Interface) built with the Bubble Tea framework for interacting with AI models to assist with coding, debugging, and development workflows. It is available as a **CLI/TUI**, a **web interface**, and an **IDE extension**.

OpenCode was created by Jay V, Frank Wang, Dax Raad, and Adam Elmore. The project has amassed over 95,000 GitHub stars and is one of the most popular open-source AI coding agents. It is provider-agnostic and privacy-first -- OpenCode does not store code or context, and users retain full control over session sharing.

<Info>
  OpenCode is model-agnostic. It supports providers including Anthropic, OpenAI, Google Gemini, AWS Bedrock, Azure OpenAI, Groq, Google Cloud Vertex AI, OpenRouter, GitHub Copilot, and local models. It also offers **OpenCode Zen**, a curated model list vetted by the team.
</Info>

### Key Features

| Feature | Description |
|---|---|
| **Interactive TUI** | Rich terminal interface with Vim-like editor, themes, and keyboard shortcuts |
| **Build and Plan Modes** | Build mode for full development; Plan mode for read-only analysis and planning |
| **MCP Support** | Extends capabilities through Model Context Protocol servers (local and remote) |
| **Multi-Provider Support** | Works with Anthropic, OpenAI, Google, AWS Bedrock, Azure, Groq, and local models |
| **Built-in Tools** | File operations, shell execution, search, web fetch, and more |
| **AGENTS.md** | Project-level instruction files for customizing AI behavior |
| **Custom Agents** | Define specialized agents with custom prompts, models, and tool access |
| **Session Management** | Persistent SQLite storage for conversations with export/import/sharing |
| **Auto Compact** | Automatically summarizes conversations when approaching context limits |
| **LSP Integration** | Language Server Protocol support for code intelligence (experimental) |
| **Plugins** | Extend OpenCode with custom tools, hooks, and integrations |

---

## Configuration

### File Locations

OpenCode uses JSON/JSONC configuration files with a merge-based system. Configuration sources are loaded in order, with later sources overriding conflicting keys.

<Tabs>
  <Tab title="macOS / Linux">
    | File | Path | Purpose |
    |---|---|---|
    | Global config | `~/.config/opencode/opencode.json` | User-wide settings (providers, models, permissions) |
    | Project config | `<project>/opencode.json` | Project-specific overrides (highest precedence) |
    | TUI config | `~/.config/opencode/tui.json` | Theme, keybinds, scroll settings |
    | Agents (global) | `~/.config/opencode/agents/` | Global custom agent definitions (markdown) |
    | Agents (project) | `<project>/.opencode/agents/` | Project-specific agent definitions |
    | Plugins (global) | `~/.config/opencode/plugins/` | Global plugin files |
    | Plugins (project) | `<project>/.opencode/plugins/` | Project-specific plugin files |
    | Modes (global) | `~/.config/opencode/modes/` | Global custom mode definitions |
    | Modes (project) | `<project>/.opencode/modes/` | Project-specific mode definitions |
    | Data directory | `<project>/.opencode/` | SQLite database, sessions, local state |
  </Tab>
  <Tab title="Windows">
    | File | Path | Purpose |
    |---|---|---|
    | Global config | `~/.config/opencode/opencode.json` | User-wide settings |
    | Project config | `<project>\opencode.json` | Project-specific overrides |
    | TUI config | `~/.config/opencode/tui.json` | Theme, keybinds, scroll settings |

    <Note>WSL is recommended for the best experience on Windows.</Note>
  </Tab>
</Tabs>

### Configuration Load Order

Settings are resolved in the following order (earliest to latest, with later sources taking precedence):

1. **Remote config** (`.well-known/opencode` endpoint for organizational defaults)
2. **Global config** (`~/.config/opencode/opencode.json`)
3. **Custom path** (`OPENCODE_CONFIG` environment variable)
4. **Project config** (`opencode.json` in project root)
5. **`.opencode/` directories** (agents, commands, plugins, etc.)
6. **Inline config** (`OPENCODE_CONFIG_CONTENT` environment variable)

### opencode.json Structure

<CodeGroup>

```json Basic Configuration
{
  "$schema": "https://opencode.ai/config.json",
  "model": "anthropic/claude-sonnet-4-5",
  "small_model": "anthropic/claude-haiku-3-5",
  "default_agent": "build",
  "autoCompact": true,
  "share": "manual",
  "instructions": [
    "docs/guidelines.md",
    "packages/*/AGENTS.md"
  ]
}
```

```json Provider Configuration
{
  "provider": {
    "anthropic": {
      "apiKey": "{env:ANTHROPIC_API_KEY}",
      "disabled": false
    },
    "openai": {
      "apiKey": "{env:OPENAI_API_KEY}",
      "disabled": false
    }
  },
  "disabled_providers": ["groq"],
  "enabled_providers": ["anthropic", "openai"]
}
```

```json Agent Configuration
{
  "agent": {
    "reviewer": {
      "model": "anthropic/claude-sonnet-4-5",
      "prompt": "{file:./prompts/reviewer.md}",
      "tools": {
        "bash": false,
        "edit": false,
        "write": false,
        "read": true,
        "grep": true,
        "glob": true
      },
      "permission": {
        "bash": "deny"
      },
      "maxSteps": 50
    }
  }
}
```

```json MCP Server Configuration
{
  "mcp": {
    "github-server": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-github"],
      "environment": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "{env:GITHUB_TOKEN}"
      },
      "timeout": 5000
    },
    "remote-server": {
      "type": "remote",
      "url": "https://mcp.example.com",
      "headers": {
        "Authorization": "Bearer {env:API_KEY}"
      }
    }
  }
}
```

</CodeGroup>

### Variable Substitution

OpenCode supports variable substitution in configuration values:

| Syntax | Description |
|---|---|
| `{env:VARIABLE_NAME}` | Replaced with the value of the environment variable |
| `{file:path/to/file}` | Replaced with the contents of the file (relative/absolute paths, tilde expansion) |

<Note>
  Configuration files use a merge strategy -- later sources override only conflicting keys, preserving the rest. Changes to config files take effect on the next OpenCode launch.
</Note>

### The AGENTS.md File

The `AGENTS.md` file provides OpenCode with custom instructions about your project. Its contents are included in the LLM context to shape behavior for your specific codebase. Use the `/init` command to auto-generate one by scanning your project.

```markdown AGENTS.md
# Project: My TypeScript App

## Stack
- TypeScript monorepo with pnpm workspaces
- Next.js for the web application
- PostgreSQL with Drizzle ORM

## Conventions
- Use strict TypeScript, no `any` types
- Prefer functional patterns over classes
- All public APIs must have JSDoc comments
- Tests use Vitest

## Commands
- Build: `pnpm build`
- Test: `pnpm test`
- Lint: `pnpm lint`
```

**File search order (first match in each category wins):**

| Scope | Location | Fallback |
|---|---|---|
| Project-level | `<project>/AGENTS.md` | `<project>/CLAUDE.md` |
| Global | `~/.config/opencode/AGENTS.md` | `~/.claude/CLAUDE.md` |

OpenCode traverses up the directory tree looking for `AGENTS.md` files. It also supports Claude Code compatibility by falling back to `CLAUDE.md` files. This fallback can be disabled with the `OPENCODE_DISABLE_CLAUDE_CODE` environment variable.

Additional instruction files can be specified in `opencode.json` using the `instructions` field, which supports glob patterns and remote URLs:

```json
{
  "instructions": [
    "CONTRIBUTING.md",
    "docs/guidelines.md",
    ".cursor/rules/*.md",
    "https://example.com/team-rules.md"
  ]
}
```

<Warning>
  Keep `AGENTS.md` focused and concise. All instruction files (AGENTS.md plus any files in the `instructions` array) are combined and included in the LLM context for every request.
</Warning>

---

## MCP Servers

OpenCode implements the Model Context Protocol (MCP) to extend its capabilities through external tools. MCP servers are configured in `opencode.json` and their tools automatically become available to the AI alongside built-in tools.

### Server Types

| Type | Description | Configuration |
|---|---|---|
| **local** | Runs a command on the local machine (stdio transport) | `command`, `environment`, `timeout` |
| **remote** | Connects to an external HTTP endpoint | `url`, `headers`, `timeout` |

### Adding MCP Servers

<Tabs>
  <Tab title="CLI">
    Run `opencode mcp add` for an interactive setup that guides you through adding either a local or remote MCP server.

    ```bash
    # Add a server interactively
    opencode mcp add

    # List configured servers
    opencode mcp list

    # Authenticate with OAuth-enabled server
    opencode mcp auth my-server

    # Debug connection issues
    opencode mcp debug my-server
    ```
  </Tab>
  <Tab title="opencode.json (Manual)">
    ```json
    {
      "mcp": {
        "github": {
          "type": "local",
          "command": ["npx", "-y", "@modelcontextprotocol/server-github"],
          "environment": {
            "GITHUB_PERSONAL_ACCESS_TOKEN": "{env:GITHUB_TOKEN}"
          },
          "timeout": 5000
        },
        "sentry": {
          "type": "remote",
          "url": "https://mcp.sentry.dev/sse"
        }
      }
    }
    ```
  </Tab>
</Tabs>

### OAuth Authentication

OpenCode handles OAuth automatically via Dynamic Client Registration (RFC 7591). Three approaches are supported:

| Approach | Description |
|---|---|
| **Automatic** | No configuration needed; auth triggers on first use |
| **Pre-registered** | Supply existing `clientId`, `clientSecret`, and `scope` |
| **Disabled** | Set `"oauth": false` for API key-based servers |

### Tool Management

MCP server tools follow the same permission model as built-in tools. You can enable or disable specific MCP tools using glob patterns:

```json
{
  "tools": {
    "my-mcp-foo": false,
    "my-mcp*": false
  }
}
```

<Info>
  MCP servers add to your context window. Be selective about which servers you enable to avoid unnecessary token usage.
</Info>

---

## Agents and Modes

### Built-in Agents

OpenCode ships with two primary agents and two subagents:

| Agent | Type | Description |
|---|---|---|
| **Build** | Primary | Default agent with all tools enabled for full development work |
| **Plan** | Primary | Restricted agent for analysis and planning; file edits and bash require approval |
| **General** | Subagent | Full-access agent for multi-step research tasks |
| **Explore** | Subagent | Read-only agent for quickly navigating codebases |

**Primary agents** are the main assistants you interact with directly. Switch between them using the `Tab` key. **Subagents** are specialized assistants invoked automatically or via `@mentions`.

### Custom Agents

Define custom agents in `opencode.json` or as markdown files in `.opencode/agents/` or `~/.config/opencode/agents/`:

```json
{
  "agent": {
    "security-reviewer": {
      "model": "anthropic/claude-sonnet-4-5",
      "prompt": "{file:./prompts/security.md}",
      "tools": {
        "bash": false,
        "write": false,
        "read": true,
        "grep": true
      },
      "permission": {
        "bash": "deny"
      },
      "temperature": 0.3,
      "maxSteps": 30
    }
  }
}
```

You can also run `opencode agent create` for an interactive setup.

### Modes

Modes are customizable configurations that tailor the AI's behavior and available tools. OpenCode includes two built-in modes:

| Mode | Behavior |
|---|---|
| **Build** | Full access to all tools for comprehensive development |
| **Plan** | Read-only; no file writing, editing, or bash execution |

Custom modes can be defined in `opencode.json` or as markdown files in `.opencode/modes/` or `~/.config/opencode/modes/`.

---

## Built-in Tools

| Tool | Description |
|---|---|
| **bash** | Execute shell commands in the project environment |
| **edit** | Modify existing files using exact string replacements |
| **write** | Create new files or overwrite existing ones |
| **read** | Read file contents (supports line ranges for large files) |
| **grep** | Search file contents using regular expressions (ripgrep) |
| **glob** | Find files by pattern matching, sorted by modification time |
| **list** | List files and directories with optional glob filtering |
| **patch** | Apply patch files to codebases |
| **webfetch** | Fetch and read web pages |
| **websearch** | Search the web via Exa AI (requires `OPENCODE_ENABLE_EXA=1`) |
| **question** | Ask the user questions during execution with structured options |
| **todowrite** | Manage todo lists during coding sessions |
| **todoread** | Read existing todo lists |
| **skill** | Load skill files (SKILL.md) into conversations |
| **lsp** | Code intelligence via Language Server Protocol (experimental, requires `OPENCODE_EXPERIMENTAL_LSP_TOOL=true`) |

### Tool Permissions

Tools support three permission levels configured in `opencode.json`:

| Permission | Behavior |
|---|---|
| `allow` | Tool runs without user confirmation |
| `deny` | Tool is blocked from execution |
| `ask` | Tool requires explicit user approval before running |

```json
{
  "permission": {
    "bash": "ask",
    "edit": "ask",
    "webfetch": "ask"
  }
}
```

---

## Model Configuration

### Supported Providers

| Category | Providers |
|---|---|
| **API-based** | Anthropic, OpenAI, Google Gemini, Groq, OpenRouter |
| **Cloud Platforms** | Amazon Bedrock, Azure OpenAI, Google Cloud Vertex AI |
| **Special** | GitHub Copilot (device flow auth), OpenCode Zen (curated models) |
| **Local** | Ollama and other local endpoints via `LOCAL_ENDPOINT` |

### Provider Environment Variables

| Provider | Required Variables | Optional Variables |
|---|---|---|
| **Anthropic** | `ANTHROPIC_API_KEY` | |
| **OpenAI** | `OPENAI_API_KEY` | |
| **Google Gemini** | `GEMINI_API_KEY` | |
| **AWS Bedrock** | `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY` | `AWS_REGION` |
| **Azure OpenAI** | `AZURE_OPENAI_ENDPOINT`, `AZURE_OPENAI_API_KEY` | `AZURE_OPENAI_API_VERSION` |
| **Groq** | `GROQ_API_KEY` | |
| **Google Vertex AI** | `VERTEXAI_PROJECT`, `VERTEXAI_LOCATION` | |
| **GitHub Copilot** | `GITHUB_TOKEN` | |
| **Local** | | `LOCAL_ENDPOINT` |

### Configuring a Provider

<Steps>
  <Step title="Authenticate with a provider">
    Run `opencode auth login` to configure API keys interactively, or set the appropriate environment variable (e.g., `ANTHROPIC_API_KEY`). Alternatively, use the `/connect` command in the TUI for OpenCode Zen authentication.
  </Step>
  <Step title="Set the model">
    Configure the model in `opencode.json` using the `provider/model-name` format:

    ```json
    {
      "model": "anthropic/claude-sonnet-4-5",
      "small_model": "anthropic/claude-haiku-3-5"
    }
    ```

    Or pass it via the CLI flag: `opencode --model anthropic/claude-sonnet-4-5`
  </Step>
  <Step title="Verify available models">
    Run `opencode models` to list all available models, or `opencode models anthropic` to filter by provider. Use `--refresh` to update the model list.
  </Step>
</Steps>

### Per-Agent Model Overrides

Different agents can use different models, allowing you to optimize for cost and capability:

```json
{
  "agent": {
    "coder": {
      "model": "anthropic/claude-sonnet-4-5",
      "maxTokens": 5000
    },
    "planner": {
      "model": "openai/o3-mini"
    }
  }
}
```

---

## CLI Commands

### Core Commands

| Command | Description |
|---|---|
| `opencode` | Start the interactive TUI (default) |
| `opencode run "message"` | Run a single prompt non-interactively |
| `opencode run --file prompt.md` | Run instructions from a file |
| `opencode serve` | Start a headless HTTP server for API access |
| `opencode web` | Start a headless server with web browser interface |
| `opencode attach [url]` | Connect TUI to a running backend |
| `opencode upgrade` | Update to the latest version |
| `opencode uninstall` | Remove OpenCode |

### Session Management

| Command | Description |
|---|---|
| `opencode --continue` | Resume the previous session |
| `opencode --session <id>` | Resume a specific session |
| `opencode --fork` | Fork an existing session |
| `opencode export [sessionID]` | Export session as JSON |
| `opencode import <file>` | Import session from file or share URL |
| `opencode session list` | List all saved sessions |
| `opencode stats` | View token usage and cost statistics |

### Agent and Model Commands

| Command | Description |
|---|---|
| `opencode agent create` | Create a new custom agent interactively |
| `opencode agent list` | Display all available agents |
| `opencode models [provider]` | List available models (optionally filter by provider) |
| `opencode auth login` | Configure API keys for providers |
| `opencode auth list` | Show authenticated providers |
| `opencode auth logout` | Remove provider credentials |

### MCP Commands

| Command | Description |
|---|---|
| `opencode mcp add` | Add an MCP server interactively |
| `opencode mcp list` | List configured MCP servers and connection status |
| `opencode mcp auth [name]` | Authenticate with an OAuth-enabled MCP server |
| `opencode mcp auth list` | List OAuth-capable servers and auth status |
| `opencode mcp logout [name]` | Remove stored OAuth credentials |
| `opencode mcp debug <name>` | Diagnose MCP server connection issues |

### Other Commands

| Command | Description |
|---|---|
| `opencode github install` | Set up GitHub Actions workflow |
| `opencode github run` | Execute agent in GitHub Actions |
| `opencode acp` | Run as an Agent Client Protocol server |

### CLI Flags

| Flag | Short | Description |
|---|---|---|
| `--help` | `-h` | Display help information |
| `--version` | `-v` | Print version number |
| `--model` | `-m` | Override the model |
| `--agent` | | Select a specific agent |
| `--prompt` | | Provide an initial prompt |
| `--continue` | `-c` | Resume the previous session |
| `--session` | `-s` | Resume a specific session by ID |
| `--fork` | | Fork an existing session |
| `--format` | | Output format for non-interactive mode (text/json) |
| `--print-logs` | | Output logs to stderr |
| `--log-level` | | Set log verbosity (DEBUG, INFO, WARN, ERROR) |

### TUI Commands

Within the interactive TUI, these commands are available:

| Command | Description |
|---|---|
| `/init` | Generate an `AGENTS.md` file for the current project |
| `/connect` | Authenticate with OpenCode Zen |
| `/undo` | Revert recent changes |
| `/redo` | Reapply reverted changes |
| `/share` | Create a shareable conversation link |
| `@` | Fuzzy-search and reference files |
| `Tab` | Switch between Build and Plan modes |

### Keyboard Shortcuts

| Context | Shortcut | Action |
|---|---|---|
| Global | `Ctrl+C` | Quit |
| Global | `Ctrl+?` | Help |
| Global | `Ctrl+L` | View logs |
| Global | `Ctrl+A` | Switch session |
| Global | `Ctrl+K` | Commands palette |
| Global | `Ctrl+O` | Model selector |
| Chat | `Ctrl+N` | New session |
| Chat | `Ctrl+X` | Cancel current operation |
| Editor | `Ctrl+S` | Send message |
| Editor | `Ctrl+E` | Open external editor |

---

## Installation

<Tabs>
  <Tab title="curl (All platforms)">
    ```bash
    curl -fsSL https://opencode.ai/install | bash
    ```
  </Tab>
  <Tab title="Homebrew (macOS / Linux)">
    ```bash
    brew install anomalyco/tap/opencode
    ```
  </Tab>
  <Tab title="npm / Node.js">
    ```bash
    npm install -g opencode-ai
    # or
    pnpm add -g opencode-ai
    # or
    bun add -g opencode-ai
    # or
    yarn global add opencode-ai
    ```
  </Tab>
  <Tab title="Go">
    ```bash
    go install github.com/opencode-ai/opencode@latest
    ```
  </Tab>
  <Tab title="Arch Linux">
    ```bash
    sudo pacman -S opencode
    # or via AUR
    paru -S opencode-bin
    ```
  </Tab>
  <Tab title="Windows">
    ```bash
    # Chocolatey
    choco install opencode

    # Scoop
    scoop install opencode
    ```

    <Note>WSL is recommended for the best experience on Windows.</Note>
  </Tab>
  <Tab title="Docker">
    ```bash
    docker run -it --rm ghcr.io/anomalyco/opencode
    ```
  </Tab>
</Tabs>

Verify installation:

```bash
opencode --version
```

---

## Installation Provider Notes

This section covers what an AIPkg installer needs to know to programmatically install and configure packages for the OpenCode platform.

### Configuration File Paths

<Tabs>
  <Tab title="macOS / Linux">
    - **Global config**: `~/.config/opencode/opencode.json`
    - **Project config**: `<project>/opencode.json`
    - **TUI config**: `~/.config/opencode/tui.json`
    - **Global agents**: `~/.config/opencode/agents/`
    - **Project agents**: `<project>/.opencode/agents/`
    - **Global plugins**: `~/.config/opencode/plugins/`
    - **Project plugins**: `<project>/.opencode/plugins/`
    - **Project instructions**: `<project>/AGENTS.md`
    - **Global instructions**: `~/.config/opencode/AGENTS.md`
  </Tab>
  <Tab title="Windows">
    - **Global config**: `~/.config/opencode/opencode.json`
    - **Project config**: `<project>\opencode.json`
    - **Project instructions**: `<project>\AGENTS.md`
  </Tab>
</Tabs>

### Installing MCP Server Extensions

To programmatically register an MCP server, merge the server definition into the `mcp` key of `opencode.json`:

```json
{
  "mcp": {
    "package-server-name": {
      "type": "local",
      "command": ["npx", "-y", "@scope/mcp-server-package"],
      "environment": {
        "SOME_API_KEY": "{env:SOME_API_KEY}"
      },
      "timeout": 5000
    }
  }
}
```

**Key considerations:**

- The `mcp` object is a flat map; each key is a unique server identifier
- Merge carefully -- do not overwrite existing MCP server entries
- The `type` field must be either `local` or `remote`
- For remote servers, use `url` and optionally `headers` instead of `command`
- Environment variables can use the `{env:VAR_NAME}` substitution syntax
- The `command` field is an array (not a string), containing the command and its arguments
- Configuration can be placed in either the global or project config file

### Installing Project Instructions

To install project-level instructions, write an `AGENTS.md` file to the project root. If an `AGENTS.md` already exists, the installer should append to it.

Alternatively, use the `instructions` field in `opencode.json` to reference additional instruction files without modifying `AGENTS.md`:

```json
{
  "instructions": [
    "docs/package-guidelines.md"
  ]
}
```

<Note>
  OpenCode supports `CLAUDE.md` as a fallback for `AGENTS.md`. If the project already has a `CLAUDE.md` file, OpenCode will use it. However, `AGENTS.md` takes precedence when both exist.
</Note>

### API Key Management

API keys can be configured via:

- **Environment variables** (e.g., `ANTHROPIC_API_KEY`) -- recommended for CI/CD and programmatic use
- **`opencode.json` config** using `{env:VAR_NAME}` substitution syntax
- **Interactive login** via `opencode auth login`

An installer should prefer setting environment variables or using the `{env:VAR_NAME}` syntax in config files rather than embedding raw keys.

### Config File Format

OpenCode uses JSON/JSONC for configuration. The installer must:

1. Parse the existing JSON file (if present)
2. Deep-merge new configuration (particularly the `mcp` map) into the existing structure
3. Write the result back, preserving existing entries

The `mcp` key is the primary integration point for MCP server packages. Provider settings go under the `provider` key. The `$schema` field (`"https://opencode.ai/config.json"`) should be preserved or added for IDE validation support.

### Tool Permissions

An installer should be aware of OpenCode's tool permission model when configuring MCP servers:

| Permission | Behavior |
|---|---|
| `allow` | Tool runs without user confirmation |
| `deny` | Tool is blocked from execution |
| `ask` | Tool requires explicit user approval before running |

MCP server tools can be selectively enabled or disabled using glob patterns in the `tools` configuration:

```json
{
  "tools": {
    "my-mcp-*": true
  }
}
```

### Non-Interactive Execution

For CI/CD or automated workflows, use `opencode run`:

```bash
# Run a single prompt
opencode run "Run the test suite and report results"

# Run with a specific model
opencode run --model anthropic/claude-sonnet-4-5 "Analyze this codebase"

# Run with JSON output
opencode run --format json "Summarize recent changes"

# Run from a file
opencode run --file ./instructions.md
```

### Environment Variables

| Variable | Purpose |
|---|---|
| `OPENCODE_CONFIG` | Custom config file path |
| `OPENCODE_CONFIG_CONTENT` | Inline JSON config content |
| `OPENCODE_CONFIG_DIR` | Custom config directory |
| `OPENCODE_DISABLE_AUTOUPDATE` | Skip auto-update checks |
| `OPENCODE_EXPERIMENTAL` | Enable experimental features |
| `OPENCODE_MODELS_URL` | Custom models configuration endpoint |
| `OPENCODE_DISABLE_CLAUDE_CODE` | Disable CLAUDE.md fallback support |
| `OPENCODE_ENABLE_EXA` | Enable web search tool |
| `OPENCODE_SERVER_PASSWORD` | Enable basic auth for server mode |
