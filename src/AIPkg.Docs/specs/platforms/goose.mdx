---
title: Goose
description: Platform specification for Goose, the open-source extensible AI agent by Block (formerly Square)
icon: bird
---

## Overview

[Goose](https://block.github.io/goose/) is an open-source, extensible AI agent developed by **Block** (the company behind Square and Cash App). Released in January 2025 under the Apache 2.0 license, Goose goes beyond code suggestions -- it can build entire projects, write and execute code, debug failures, orchestrate workflows, and interact with external APIs autonomously. It is available as both a **CLI** and a **Desktop application** for macOS, Linux, and Windows.

Goose is deeply built on the [Model Context Protocol (MCP)](https://modelcontextprotocol.io/). In Goose's terminology, MCP servers are called **extensions**. In December 2025, Block contributed Goose to the Linux Foundation's Agentic AI Foundation alongside Anthropic's MCP and OpenAI's AGENTS.md.

<Info>
  Goose is model-agnostic. It supports 30+ LLM providers including Anthropic, OpenAI, Google Gemini, AWS Bedrock, Ollama, and many more. It works best with models that have strong tool-calling capabilities.
</Info>

### Key Features

| Feature | Description |
|---|---|
| **Agentic Execution** | Autonomously runs shell commands, edits files, executes code, and orchestrates multi-step workflows |
| **MCP-Native Architecture** | Extensions are MCP servers; connects to 3,000+ available MCP servers |
| **Model Flexibility** | Works with any LLM provider including local models via Ollama or Docker Model Runner |
| **Recipes** | YAML-based shareable workflow definitions with parameters, sub-recipes, and scheduling |
| **Multi-Model Support** | Lead/worker model configurations and planning mode for specialized task handling |
| **Desktop and CLI** | Full-featured desktop app and powerful command-line interface |
| **Built-in Extensions** | Developer tools and Computer Controller ship out of the box |
| **.goosehints** | Project-level instruction files appended to the system prompt |

---

## Configuration

### File Locations

Goose uses YAML configuration files with a hierarchical system. Both the Desktop app and CLI share the same configuration.

<Tabs>
  <Tab title="macOS / Linux">
    | File | Path | Purpose |
    |---|---|---|
    | `config.yaml` | `~/.config/goose/config.yaml` | Primary configuration (provider, model, extensions, settings) |
    | `permission.yaml` | `~/.config/goose/permission.yaml` | Tool permission levels |
    | `secrets.yaml` | `~/.config/goose/secrets.yaml` | API keys (file-based fallback when keyring unavailable) |
    | `tool_permissions.json` | `~/.config/goose/permissions/tool_permissions.json` | Runtime permission decisions (auto-managed) |
    | Custom providers | `~/.config/goose/custom_providers/*.json` | Custom LLM provider definitions |
    | Prompt templates | `~/.config/goose/prompts/` | Custom prompt templates |
    | Sessions database | `~/.config/goose/sessions.db` | SQLite session storage (since v1.10.0) |
  </Tab>
  <Tab title="Windows">
    | File | Path | Purpose |
    |---|---|---|
    | `config.yaml` | `%APPDATA%\Block\goose\config\config.yaml` | Primary configuration |
    | Custom providers | `%APPDATA%\Block\goose\config\custom_providers\` | Custom LLM provider definitions |
  </Tab>
</Tabs>

### config.yaml Structure

<CodeGroup>

```yaml Basic Configuration
# Provider and model
GOOSE_PROVIDER: anthropic
GOOSE_MODEL: claude-sonnet-4-20250514

# Execution behavior
GOOSE_MODE: auto          # auto | approve | chat | smart_approve
GOOSE_MAX_TURNS: 1000     # Max turns without user input
GOOSE_TEMPERATURE: 0.5    # Response randomness (0.0-1.0)

# CLI display
GOOSE_CLI_THEME: dark     # light | dark | ansi

# Security
SECURITY_PROMPT_ENABLED: true
SECURITY_PROMPT_THRESHOLD: 0.5  # 0.01-1.0
```

```yaml Extensions Configuration
extensions:
  developer:
    bundled: true
    enabled: true
    name: developer
    timeout: 300
    type: builtin

  my-mcp-server:
    enabled: true
    name: my-mcp-server
    type: stdio
    cmd: npx
    args:
      - "-y"
      - "@my-org/mcp-server"
    envs:
      API_KEY: "your-key-here"
    timeout: 300
    description: "My custom MCP server"
```

```yaml Advanced Multi-Model Configuration
# Lead/Worker mode
GOOSE_LEAD_PROVIDER: anthropic
GOOSE_LEAD_MODEL: claude-sonnet-4-20250514

# Planning mode
GOOSE_PLANNER_PROVIDER: openai
GOOSE_PLANNER_MODEL: o3-mini

# Tool interpretation
GOOSE_TOOLSHIM: true

# Search paths for tools
GOOSE_SEARCH_PATHS:
  - "/usr/local/bin"
  - "~/custom/tools"
  - "/opt/homebrew/bin"
```

</CodeGroup>

### Configuration Priority

Settings are resolved in the following order (highest to lowest):

1. **Environment variables** (e.g., `GOOSE_PROVIDER=anthropic`)
2. **Config file** (`~/.config/goose/config.yaml`)
3. **Default values**

<Note>
  Changes to config files require restarting Goose to take effect. Use `goose info -v` to display active configuration and verify current values.
</Note>

### The .goosehints File

The `.goosehints` file provides Goose with supplemental context about your project. Its contents are appended to the system prompt at the start of each session.

```text .goosehints
This is a TypeScript monorepo using pnpm workspaces.

To run tests: pnpm test
To build: pnpm build

Key modules:
- packages/core: Core library
- packages/cli: Command-line interface
- apps/web: Next.js web application

Coding conventions:
- Use strict TypeScript with no `any` types
- Prefer functional patterns over classes
- All public APIs must have JSDoc comments
```

**Placement locations:**

| Scope | Location |
|---|---|
| Project-wide | `<project-root>/.goosehints` |
| User-global | `~/.goosehints` |
| Directory-specific | `<subdirectory>/.goosehints` |

Goose supports hierarchical `.goosehints` files at different levels of a project. Multiple files are combined.

<Warning>
  Keep `.goosehints` minimal. For extensive conventions (200+ lines), consider using the Memory Extension MCP server instead, which retrieves detailed standards on-demand rather than including them in every request.
</Warning>

---

## MCP Servers / Extensions

Goose's extension system is built entirely on the Model Context Protocol. In Goose, every extension is an MCP server -- there is no separate plugin format.

### Extension Types

| Type | Description | Configuration |
|---|---|---|
| **builtin** | Core extensions bundled with Goose | Enabled via `config.yaml` or `goose configure` |
| **stdio** | Local MCP servers communicating over standard I/O | Command, args, and env vars in `config.yaml` |
| **sse** | Remote MCP servers using Server-Sent Events | URI endpoint in `config.yaml` |
| **streamable_http** | Remote MCP servers using HTTP streaming | URI endpoint in `config.yaml` |

### Built-in Extensions

Goose ships with two built-in extensions:

| Extension | Description |
|---|---|
| **Developer** | Core development tools: file operations, shell command execution, code editing |
| **Computer Controller** | Browser automation and system interaction capabilities |

### Adding Extensions

<Tabs>
  <Tab title="CLI (goose configure)">
    Run `goose configure` and select the extensions menu. For a command-line (stdio) extension, provide:
    - **Name**: A human-readable label (e.g., "filesystem")
    - **Command**: The command that starts the MCP server
    - **Args**: Arguments for the server command
    - **Timeout**: Start with 300 seconds; increase for slow-starting servers
  </Tab>
  <Tab title="Desktop App">
    Go to **Settings > Extensions > Add custom extension** and fill in the required fields in the form.
  </Tab>
  <Tab title="config.yaml (Manual)">
    ```yaml
    extensions:
      github:
        enabled: true
        name: github
        type: stdio
        cmd: npx
        args:
          - "-y"
          - "@modelcontextprotocol/server-github"
        envs:
          GITHUB_PERSONAL_ACCESS_TOKEN: "ghp_..."
        timeout: 300

      remote-server:
        enabled: true
        name: remote-server
        type: sse
        uri: "https://mcp.example.com/sse"
        timeout: 300
    ```
  </Tab>
</Tabs>

### Dynamic Extension Discovery

Goose includes an **Extensions Manager** that allows the agent to search for and enable relevant MCP servers dynamically during a session, even for servers that require authentication (as long as they are pre-configured).

### MCP Sampling

MCP Sampling allows your MCP servers to request AI completions from Goose's LLM, transforming simple tool servers into intelligent agents that can leverage the model for their own processing.

---

## Recipes

Recipes are YAML (or JSON) workflow definitions that bundle instructions, extensions, parameters, and execution logic into shareable, reproducible configurations. They are one of Goose's most distinctive features.

### Recipe Structure

```yaml example-recipe.yaml
version: 1.0.0
title: "Code Review Pipeline"
description: "Automated code review with security analysis"
author:
  contact: "your-github-username"

# System prompt for the agent
instructions: |
  You are a senior code reviewer. Analyze the provided repository
  for code quality, security issues, and adherence to best practices.

# Settings override
settings:
  goose_provider: "anthropic"
  goose_model: "claude-sonnet-4-20250514"
  temperature: 0.3
  max_turns: 50

# Typed input parameters
parameters:
  - key: repository_path
    input_type: string
    requirement: required
    description: "Path to the repository to analyze"
  - key: scan_level
    input_type: string
    requirement: optional
    default: "standard"
    description: "Depth of review: quick, standard, thorough"

# Extensions required by this recipe
extensions:
  - type: builtin
    name: developer
    display_name: Developer
    timeout: 300
    bundled: true

# Initial prompt (required for headless/non-interactive mode)
prompt: |
  Analyze the project at {{ repository_path }} using a {{ scan_level }} scan.

# Optional UI activities (Desktop only)
activities:
  - "Scanning repository structure"
  - "Running security checks"
  - "Generating report"

# Sub-recipes for decomposed tasks
sub_recipes:
  - name: "security_audit"
    path: "./sub-recipes/security.yaml"
    values:
      check_cors: "true"
  - name: "style_check"
    path: "./sub-recipes/style.yaml"

# Retry logic
retry:
  max_retries: 3
  checks:
    - type: shell
      command: "test -f report.md"
      on_failure: "rm -f temp_*"

# Structured output schema
response:
  json_schema:
    type: object
    properties:
      summary:
        type: string
      issues_found:
        type: integer
      critical_issues:
        type: array
        items:
          type: string
    required:
      - summary
      - issues_found
```

### Parameter Substitution

Parameters use `{{ parameter_name }}` syntax and are substituted into `instructions`, `prompt`, and `activities` fields before the recipe starts.

### Sub-Recipes

Sub-recipes run in isolated sessions with their own context. When a main recipe runs, Goose generates a tool for each sub-recipe that accepts defined parameters, executes in a separate session, and returns output to the main recipe.

<Warning>
  Sub-recipes cannot define their own sub-recipes. Only one level of nesting is allowed.
</Warning>

### Running Recipes

```bash
# Run a recipe file
goose run --recipe ./my-recipe.yaml

# Run with parameters
goose run --recipe ./my-recipe.yaml --params repository_path=/home/user/project

# Open in Desktop
goose recipe open ./my-recipe.yaml

# Validate syntax
goose recipe validate ./my-recipe.yaml

# Generate a shareable deeplink
goose recipe deeplink my-recipe

# List available recipes
goose recipe list
```

### Scheduling Recipes

```bash
# Schedule a recipe with cron
goose schedule add \
  --schedule-id daily-report \
  --cron "0 0 9 * * *" \
  --recipe-source ./recipes/daily-report.yaml

# List scheduled jobs
goose schedule list

# Remove a scheduled job
goose schedule remove --schedule-id daily-report
```

### Slash Command Recipes

Recipes can be bound to slash commands in `config.yaml`:

```yaml
slash_commands:
  - command: "onboarding"
    recipe_path: "/path/to/onboarding-recipe.yaml"
```

This allows users to type `/onboarding` in a session to execute the recipe.

---

## Model Configuration

### Supported Providers

Goose supports 30+ LLM providers across three categories:

| Category | Providers |
|---|---|
| **API-based** | Anthropic, OpenAI, Google Gemini, xAI, Mistral AI, Groq, DeepSeek, OpenRouter |
| **Cloud Platforms** | Amazon Bedrock, GCP Vertex AI, Azure OpenAI, Databricks, Snowflake |
| **Local** | Ollama, Ramalama, LM Studio, Docker Model Runner |
| **Special** | GitHub Copilot (device flow auth) |

### Provider Environment Variables

| Provider | Required Variables | Optional Variables |
|---|---|---|
| **Anthropic** | `ANTHROPIC_API_KEY` | `ANTHROPIC_HOST` |
| **OpenAI** | `OPENAI_API_KEY` | `OPENAI_HOST`, `OPENAI_ORGANIZATION`, `OPENAI_PROJECT`, `OPENAI_CUSTOM_HEADERS` |
| **Google Gemini** | `GOOGLE_API_KEY` | `GEMINI3_THINKING_LEVEL` |
| **Azure OpenAI** | `AZURE_OPENAI_ENDPOINT`, `AZURE_OPENAI_DEPLOYMENT_NAME` | `AZURE_OPENAI_API_KEY` (or credential chain) |
| **AWS Bedrock** | `AWS_REGION` | `AWS_PROFILE` or `AWS_ACCESS_KEY_ID` + `AWS_SECRET_ACCESS_KEY` |
| **Groq** | `GROQ_API_KEY` | |
| **Mistral** | `MISTRAL_API_KEY` | |
| **Ollama** | (none) | `OLLAMA_HOST` (defaults to localhost) |

### Configuring a Provider

<Steps>
  <Step title="Run goose configure">
    Execute `goose configure` in the CLI (or open the Models tab in the Desktop app). Select your provider from the filterable list.
  </Step>
  <Step title="Enter API Key">
    Provide your API key when prompted. Goose stores keys in the system keyring by default (macOS Keychain, Windows Credential Manager). If a keyring is unavailable, it falls back to `~/.config/goose/secrets.yaml`.
  </Step>
  <Step title="Select a Model">
    Choose a model from the available options for your provider. You can also type a custom model name if it is not listed.
  </Step>
</Steps>

### Custom Providers

Define custom OpenAI-compatible providers by placing a JSON file in `~/.config/goose/custom_providers/`:

```json my-provider.json
{
  "name": "my_provider",
  "engine": "openai",
  "display_name": "My Custom Provider",
  "api_key_env": "MY_PROVIDER_API_KEY",
  "base_url": "https://api.my-provider.com/v1",
  "models": [
    {
      "name": "my-model-v1",
      "context_limit": 128000
    }
  ],
  "headers": {},
  "supports_streaming": true,
  "requires_auth": true
}
```

### Multi-Model Configurations

Goose supports two advanced multi-model modes:

| Mode | Environment Variables | Description |
|---|---|---|
| **Lead/Worker** | `GOOSE_LEAD_PROVIDER`, `GOOSE_LEAD_MODEL` | Uses a lead model for planning and a worker model for execution |
| **Planning Mode** | `GOOSE_PLANNER_PROVIDER`, `GOOSE_PLANNER_MODEL` | Separate model specifically for planning steps before acting |

---

## CLI Commands

### Core Commands

| Command | Description |
|---|---|
| `goose session` | Start an interactive chat session |
| `goose session --resume` | Resume the previous session |
| `goose session --session-id <id>` | Resume a specific session by ID |
| `goose session --name <name>` | Name the session |
| `goose session --fork` | Duplicate a session with its history |
| `goose run -t "instructions"` | Run a one-shot task non-interactively |
| `goose run -i instructions.md` | Run instructions from a file |
| `goose run --recipe recipe.yaml` | Run a recipe file |
| `goose configure` | Interactive configuration wizard |
| `goose info` | Show version, config path, session storage, and logs |
| `goose info -v` | Show detailed configuration including env vars and extensions |
| `goose version` | Print the installed version |
| `goose update` | Update to the latest stable version |
| `goose update --canary` | Update to the latest canary (development) version |

### Session Management

| Command | Description |
|---|---|
| `goose session list` | List all saved sessions |
| `goose session list -f json` | List sessions in JSON format |
| `goose session remove --session-id <id>` | Delete a specific session |
| `goose session remove -r "<regex>"` | Delete sessions matching a pattern |
| `goose session export -o file.md` | Export session (json, yaml, or markdown) |
| `goose session diagnostics` | Generate a troubleshooting ZIP bundle |

### Project Management

| Command | Description |
|---|---|
| `goose project` | Start working on your last project or create a new one |
| `goose project list` | Choose from your existing projects |

### Recipe and Schedule Commands

| Command | Description |
|---|---|
| `goose recipe list` | List available recipes |
| `goose recipe validate <file>` | Validate recipe syntax |
| `goose recipe open <file>` | Open recipe in Desktop |
| `goose recipe deeplink <name>` | Generate a shareable link |
| `goose schedule add` | Create a scheduled recipe job |
| `goose schedule list` | View all scheduled jobs |
| `goose schedule remove` | Delete a scheduled job |

### Other Commands

| Command | Description |
|---|---|
| `goose web --open` | Start the web interface and open in browser |
| `goose web --port 8080` | Start web interface on a custom port |
| `goose bench` | Evaluate system configuration across tasks |
| `goose mcp` | Run an enabled MCP server by name |
| `goose acp` | Run as an Agent Client Protocol server over stdio |
| `goose completion <shell>` | Generate shell completions (bash, zsh, fish, powershell, elvish) |

### Interactive Session Commands

Within an active session, these slash commands are available:

| Command | Description |
|---|---|
| `/?` or `/help` | Display help |
| `/clear` | Clear chat history |
| `/mode <name>` | Set execution mode |
| `/plan <message>` | Enter planning mode |
| `/recipe [filepath]` | Generate a recipe from the conversation |
| `/t` | Toggle theme (light/dark/ansi) |
| `/builtin <names>` | Add built-in extensions mid-session |
| `/prompts` | List available prompt templates |

### Terminal Integration

After setting up terminal integration, you can invoke Goose directly from your shell prompt:

```bash
@goose create a python script that parses CSV files
@g how do I fix these errors?
```

Command history is automatically included in the context.

---

## Extension Development

Extensions in Goose are standard MCP servers. You can build them in any language that has an MCP SDK (Python, TypeScript, Kotlin, etc.).

<Steps>
  <Step title="Create an MCP Server">
    Use the MCP SDK for your language. The official tutorial uses Python with `uv`:

    ```bash
    uv init my-extension
    cd my-extension
    uv add mcp
    ```

    Implement your server with tools, resources, and/or prompts using the MCP SDK.
  </Step>
  <Step title="Test with MCP Inspector">
    Use the MCP Inspector (a browser-based development tool) to verify your server works correctly before integrating with Goose.

    ```bash
    npx @modelcontextprotocol/inspector uv run my-extension
    ```
  </Step>
  <Step title="Register in Goose">
    Add your extension to Goose via `goose configure` or by editing `config.yaml`:

    ```yaml
    extensions:
      my-extension:
        enabled: true
        name: my-extension
        type: stdio
        cmd: uv
        args: ["run", "my-extension"]
        timeout: 300
    ```
  </Step>
  <Step title="Iterate">
    After code changes, reinstall your package (`uv pip install .`) and restart Goose to pick up the new version.
  </Step>
</Steps>

<Info>
  MCP Sampling allows your extension to request AI completions from Goose's configured LLM, enabling extensions to leverage the model for their own intelligent processing beyond simple tool execution.
</Info>

---

## Installation

<Tabs>
  <Tab title="macOS">
    **CLI (curl):**
    ```bash
    curl -fsSL https://github.com/block/goose/releases/download/stable/download_cli.sh | bash
    ```

    **Desktop (Homebrew):**
    ```bash
    brew install --cask block-goose
    ```

    **Non-interactive install (no configure prompt):**
    ```bash
    curl -fsSL https://github.com/block/goose/releases/download/stable/download_cli.sh | CONFIGURE=false bash
    ```
  </Tab>
  <Tab title="Linux">
    **CLI (curl):**
    ```bash
    curl -fsSL https://github.com/block/goose/releases/download/stable/download_cli.sh | bash
    ```

    **Desktop:** Available as DEB (Ubuntu/Debian), RPM (RHEL/Fedora), and Flatpak packages from the [releases page](https://github.com/block/goose/releases).
  </Tab>
  <Tab title="Windows">
    **CLI (Git Bash recommended):**
    ```bash
    curl -fsSL https://github.com/block/goose/releases/download/stable/download_cli.sh | bash
    ```

    **Desktop:** Download and unzip from the [releases page](https://github.com/block/goose/releases).

    Prerequisites: Git Bash (recommended, bundled with Git for Windows) which includes `curl` and `unzip`.
  </Tab>
</Tabs>

Verify installation:

```bash
goose --version
```

---

## Installation Provider Notes

This section covers what an AIPkg installer needs to know to programmatically install and configure packages for the Goose platform.

### Configuration File Paths

<Tabs>
  <Tab title="macOS / Linux">
    - **Primary config**: `~/.config/goose/config.yaml`
    - **Secrets**: `~/.config/goose/secrets.yaml`
    - **Custom providers**: `~/.config/goose/custom_providers/<name>.json`
    - **Project hints**: `<project>/.goosehints`
    - **Global hints**: `~/.goosehints`
  </Tab>
  <Tab title="Windows">
    - **Primary config**: `%APPDATA%\Block\goose\config\config.yaml`
    - **Custom providers**: `%APPDATA%\Block\goose\config\custom_providers\<name>.json`
    - **Project hints**: `<project>\.goosehints`
  </Tab>
</Tabs>

### Installing MCP Server Extensions

To programmatically register an MCP server, merge the extension definition into the `extensions` key of `config.yaml`:

```yaml
extensions:
  package-server-name:
    enabled: true
    name: package-server-name
    type: stdio
    cmd: npx
    args:
      - "-y"
      - "@scope/mcp-server-package"
    envs:
      SOME_API_KEY: "value"
    timeout: 300
    description: "Description of the extension"
```

**Key considerations:**

- The `extensions` object is a flat map; each key is a unique extension identifier
- Merge carefully -- do not overwrite existing extension entries
- Goose requires a restart to pick up config file changes (it does not hot-reload)
- The `type` field must be one of: `builtin`, `stdio`, `sse`, or `streamable_http`
- For remote servers, use `uri` instead of `cmd`/`args`

### Installing Project Instructions

To install project-level instructions, write a `.goosehints` file to the project root. If a `.goosehints` file already exists, the installer should append to it.

<Note>
  Unlike some other platforms, Goose combines multiple `.goosehints` files hierarchically rather than using only the first match. Appending to an existing file is safe.
</Note>

### API Key Management

API keys are stored in the system keyring by default. If the keyring is unavailable, Goose falls back to `~/.config/goose/secrets.yaml`. An installer can:

- Set environment variables in the user's shell profile (e.g., `ANTHROPIC_API_KEY`)
- Write to `secrets.yaml` as a fallback (less secure)
- Instruct the user to run `goose configure` to enter keys interactively

### Config File Format

Goose uses standard YAML for `config.yaml`. The installer must:

1. Parse the existing YAML file
2. Deep-merge new configuration (particularly the `extensions` map) into the existing structure
3. Write the result back, preserving existing entries

The `extensions` key is the primary integration point for MCP server packages. Provider settings use top-level keys prefixed with `GOOSE_` (e.g., `GOOSE_PROVIDER`, `GOOSE_MODEL`).

### Execution Modes

An installer should be aware of Goose's execution modes, which control tool approval behavior:

| Mode | Behavior |
|---|---|
| `auto` | Tools run without user confirmation |
| `approve` | Every tool invocation requires explicit user approval |
| `smart_approve` | Goose decides which tools need approval based on risk |
| `chat` | Conversation only, no tool execution |

### Non-Interactive Execution

For CI/CD or automated workflows, use `goose run`:

```bash
# Run instructions from text
goose run -t "Run the test suite and report results"

# Run from a file
goose run -i ./instructions.md

# Run a recipe with parameters
goose run --recipe ./recipe.yaml --params key=value

# Run without saving a session
goose run --no-session -t "one-off task"
```
